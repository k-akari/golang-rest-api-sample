name: test
on:
  push:
    branches:
      - main
  pull_request:
    paths:
      - 'cmd/**'
      - 'internal/**'
      - 'go.mod'
      - 'go.sum'
      - 'docker/*'
      - '.devcontainer/*'

jobs:
  test:
    runs-on: ubuntu-latest
    container:
      image: golang:1.21.4
      env:
        DB_HOST: db
        DB_PORT: 3306
        REDIS_HOST: redis
        REDIS_PORT: 6379
      ports:
        - 80
    services:
      db:
        image: mysql:8.0.32
        options: >-
          --health-cmd "mysqladmin ping -h localhost"
          --health-interval 20s
          --health-timeout 10s
          --health-retries 10
        ports:
          - 3306:3306
        env:
          MYSQL_PASSWORD: password
          MYSQL_ROOT_HOST: '%'
          MYSQL_ROOT_PASSWORD: password
          MYSQL_USER: user
      redis:
        image: redis:7.2.3
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 20s
          --health-timeout 10s
          --health-retries 10
        ports:
          - 6379:6379
    steps:
    - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
    - name: Setup .pem files
      run: |
        echo "$SECRET_KEY" > internal/auth/cert/secret.pem
        echo "$PUBLIC_KEY" > internal/auth/cert/public.pem
      env:
        SECRET_KEY: ${{ secrets.SECRET_KEY_CI }}
        PUBLIC_KEY: ${{ secrets.PUBLIC_KEY_CI }}
    - run: go test ./... -coverprofile=coverage.out
    - name: report coverage
      uses: k1LoW/octocov-action@a167dc0dee441b7ffc45e1b862ab55ec0d87f278 # v0.54.6
